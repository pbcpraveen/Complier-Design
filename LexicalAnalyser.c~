#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<ctype.h>

struct Node {
  char identifier[50];
  char type[10];
  int size;
  int address;
  char value[100];
};
typedef struct Node node;

char keyword[40][20] = {"auto" , "break" , "case" , "char" , "const" , "continue" , "default" , "do" , "double" , "else" , "enum" , "extern" , "float",
			"for" , "goto" , "if" , "int" , "long" , "register" , "return" , "short" , "signed" , "sizeof" , "static" , "struct" , "switch",
			"typedef" , "union" , "unsigned" , "void" , "volatile" , "while"};
int main(){
  int f = open("code.c","r");
  char code[5000];
  int n = read(f,code,4999);
  char *line = strtok(code,'\n');

  while(line!=NULL){
    printf("%s\n",line);
    line = strtok(NULL,'\n');
  }
  
}
